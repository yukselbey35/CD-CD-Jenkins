WITH Jenkins BuildAndDeployJob configuration JOB>

We are PULLING code from github with github(Source Code Managment > Git)
Using Repository url

We are running Poll SCM to check github repo updated or not (Build Triggers)

We are building clean install (Build)

(Post-build Actions)
We will use Publish Over SSH to copy artifact from Jenkins onto Dockerhost

AND SOMETIMES USE OLD JOB TO CREATE BETTER JOB
to do that use "copy from" BuildAndDeployJob

(Build)
We will use "Send build arficats over SSH
Automatically will show our dockerhost server

This job will pull the code from github repo and build artifact 
then send this artifact to dockerhost dockeradmin home directory > 
/home/dockeradmin

